<p style="color: green"><%= notice %></p>
<%= button_to "Destroy this genere", @genere, method: :delete, class: "btn btn-danger" %>

<div id="uploads">
  <%# @uploads.in_groups_of(10, false).each do |group| %>
    <div class='row'>
      <%# group.each do |upload| %>
        <div class='col-md-2'>
          <%# <%= link_to genere.name, genere, class: 'btn btn-warning' %>
          <%= image_tag("https://pixabay.com/photos/tree-sunset-clouds-sky-silhouette-736885/") %>
        </div>
      <%# end %>
    </div>
  <%# end %>
</div>

<div>
  <%= link_to "Edit this genere", edit_genere_path(@genere) %> |
  <%= link_to "Back to generes", generes_path %>
</div>
