<!DOCTYPE html>
<html>
  <head>
    <title>StimulusHotwireDemoApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container">
      <% if notice %>
        <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert alert-danger"><%= alert %></p>
      <% end %>
      <% if user_signed_in? %>
        Logged in as <strong><%= current_user.email %></strong>.
        
        <%= link_to "Log Out", destroy_user_session_path, 'data-turbo-method': :delete, 'data-turbo-confirm': "Do you want to Logout?", class: 'btn btn-warning'  %>
      <% else %>
        <%= link_to "Sign up", new_user_registration_path, class: 'navbar-link'  %> |
        <%= link_to "Sign in", new_user_session_path, class: 'navbar-link', data: { 'turbo-action': "replace" }  %>
      <% end %>
      <%= yield %>
    </div>
  </body>
</html>
